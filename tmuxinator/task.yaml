name: <%= @args[0] || "task" %>
root: <%= @args[1] || ENV["PWD"] %>

windows:
  - agent:
      - <%= @settings["agent"] || ENV["MUX_DEFAULT_AGENT"] || "claude" %>
  - editor:
      - nvim
  - lazygit:
      - lazygit
  - shell:
